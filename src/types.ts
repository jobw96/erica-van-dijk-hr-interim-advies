export interface ServiceItem {
  title: string;
  description: string;
}

export interface VideoItem {
  date: string;
  title: string;
  placeholderUrl: string;
  videoUrl: string;
}

export interface NavItem {
  label: string;
  href: string;
}
